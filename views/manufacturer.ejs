<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= manufacturer.name %></title>
</head>
<body>
	<h1><%= manufacturer.name %></h1>
	<ul>
		<% cars.forEach(car => { %>
			<li>
				<a href="/<%= manufacturer.name %>/<%= car.id %>">
					<%= car.name %>
				</a>
			</li>
		<% }) %>
	</ul>
</body>
</html>